{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "Schema for Response Evaluation in osiris.json data files",
  "title": "ResponseEvaluation"
  "properties": {
    "Date": {
        "type": "string"
    },

    "Status": {
        "type": "string",
        "enum": ["CR", "PR", "SD", "PD"]
    }
  }
}